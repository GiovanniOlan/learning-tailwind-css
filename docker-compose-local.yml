version: '3.9'

services:
  web:
    build: 
      context: .
      dockerfile: Dockerfile-local
    # command: python manage.py runserver 0.0.0.0:8000
    ports:
      - "4200:4200"
    volumes:
      - .:/code
    restart: no